<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user_mode_capstone">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.User_Mode_capstone">
        <activity android:name=".scan_code_update"></activity>
        <activity android:name=".scan_code_add" />
        <activity android:name=".scan_code_inv" />
        <activity android:name=".scanCode_del" />
        <activity android:name=".sales_report" />
        <activity android:name=".Inventory" />
        <activity android:name=".Home_Page" />
        <activity android:name=".Delete_Item" />
        <activity android:name=".AddItem" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>